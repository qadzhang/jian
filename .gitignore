# === Maven 构建产物(不入库)===
target/
tmp_jar/

# shade 插件自动生成的精简 pom(每次 build 重新生成,不入库)
dependency-reduced-pom.xml

# === Maven Wrapper 临时 ===
.mvn/wrapper/MavenWrapperDownloader.java.bak

# === IDE ===
.idea/
*.iml
.vscode/
*.ipr
*.iws
.eclipse/
.classpath
.project
.settings/
nbproject/

# === 操作系统 ===
.DS_Store
Thumbs.db

# === 日志与临时 ===
*.log
*.tmp
*.bak
*.swp
*~

# === 敏感配置(遵循 AGENTS.md §0.2/§7.3,凭据走 .env 不入库)===
.env
.env.*
!.env.example

# === 一次性验证脚本与临时数据(AGENTS.md §6.1 任务结束清理)===
scratch/
demo-output/

# === tests-pbt 跨语言 PBT 桥编译产物(javac 生成,不入库)===
tests-pbt/classes/

# === AI/Agent 工具运行时产物(本机环境产物,与项目本身无关,不入库)===
# ZCode/Agent 会话镜像
.omo/
.claude/
.zcode/
# AI agent 工作树镜像
.claude/worktrees/

# === 测试框架运行时缓存(可重建,不入库)===
# jqwik(PBT)失败用例数据库
.jqwik-database
.jqwik-database.lock
# Python Hypothesis(PBT)缓存
.hypothesis/
# pytest 缓存
.pytest_cache/
__pycache__/
*.pyc
*.pyo

# AI 协作会话遗留文件(审计报告/调度状态,不入库)
*测试结果.md
.cli-ai-sessions.md
.cli-ai-aliases.json
